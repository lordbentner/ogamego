<!DOCTYPE html>
<html lang="fr">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <link rel="stylesheet" href="/css/bootstrap-4.3.1-dist/css/bootstrap.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel = "icon" href = "/images/icon.jpg" type = "image/x-icon">
    <title>Ogame Bot</title>
</head>
<body class="h-100">
<nav class="nav nav-pills nav-pill font-weight-bold bg-info">
    <a class="nav-item nav-link" href="/databoard">Tableau de bord</a>
    <a class="nav-item nav-link active" href="/rapports">Rapport</a>
    <a class="nav-item nav-link" href="/flottes">Flottes info</a>
    <span class="navbar-text">Temps de reconnection: {{.time_con}}</span>
</nav>
<div class="card h-100 d-flex flex-column align-items-center rapport">
    <div class="card-header text-center text-white"><h4>Nombre et infos des rapport: {{ len .spy}}</h4></div>
    <nav aria-label="Page navigation example">
        <ul class="pagination">
            {{range .nbPages}}
                <li class="page-item {{if eq $.Page .}}active{{end}}"><a class="page-link" href="/rapports?page={{.}}">
                {{.}}</a></li>
            {{end}}
        </ul>
    </nav>
    <div class="card-body">
        <table class="table table-ordered table-dark text-center">
        <thead>
            <tr>
                <th scope="row">Coordonnées</th>
                <th scope="row">Ressources</th>
            </tr>
        </thead>
        <tbody>
            {{range $k,$v := .spy}}
                {{ if and . (and (le $.fElem $k) (ge $.lElem $k))}}
                <tr>
                    <td>{{range $key,$val := .Coordinate }}
                            {{$key}}: {{$val}} - 
                        {{end}}
                    </td>
                    <td>{{range $key,$val := .Resources }}
                            {{$key}}: {{$val}} -
                        {{end}}
                    </td>
                </tr>
                {{end}}
            {{end}}
        </tbody>
        </table>
    </div>
</div>
</body>
</html>