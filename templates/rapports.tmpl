<!DOCTYPE html>
<html lang="fr">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <link rel="stylesheet" href="/css/bootstrap-4.3.1-dist/css/bootstrap.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel = "icon" href = "/images/icon.jpg" type = "image/x-icon">
    <title>Ogame Bot</title>
</head>
<body class="h-100">
<nav class="nav nav-pills nav-pill font-weight-bold bg-info justify-content-between">
    <a class="nav-item nav-link" href="/databoard">Tableau de bord</a>
    <a class="nav-item nav-link active" href="/rapports">Rapport</a>
    <a class="nav-item nav-link" href="/flottes">Flottes info</a>
    <span class="navbar-text">Temps de connexion: {{.time_con}}</span>
    <span class="navbar-text">{{.point}} points</span>
    <span class="navbar-text text-lg-right">Connecté en tant que {{.user}}</span>
    <a class="navbar-brand nav-link bg-danger" href="/quit">Quitter</a>
</nav>
<div class="card h-100 d-flex flex-column align-items-center rapport">
    <div class="card-header text-center text-white"><h4>Nombre et infos des attaques reçues: {{ len .spy}}</h4></div>
    <nav aria-label="Page navigation example">
        <ul class="pagination">
            {{range .nbPages}}
                <li class="page-item {{if eq $.Page .}}active{{end}}"><a class="page-link" href="/rapports?page={{.}}">
                {{.}}</a></li>
            {{end}}
        </ul>
    </nav>
    <div class="card-body">
        <table class="table table-ordered table-dark text-center">
        <thead>
            <tr>{{if gt (len .spy) 0}}
                    {{range $key, $val := index .spy 0}}
                        <th scope="row">{{$key}}</th>
                    {{end}}
                {{end}}
            </tr>
        </thead>
        <tbody>
            {{range $ind,$v := .spy}}
                {{if and (lt $ind $.lElem) (ge $ind $.fElem)}}
                <tr>{{range $key,$val := $v }}
                    <td>
                         {{if eq (printf "%T" $val) "map[string]interface {}"}}
                            {{range $cle, $value := $val}}
                                 {{if ne $value 0}}
                                    <p>{{$cle}}: {{$value}}</p>
                                {{end}}
                            {{end}}
                           
                        {{else}}
                            <p> {{.}}</p>
                        {{end}}
                    </td>
                    {{end}}
                </tr>
                {{end}}
            {{end}}
        </tbody>
        </table>
    </div>
</div>
</body>
</html>